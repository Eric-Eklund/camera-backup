source = "G:\\"               # Camera / memory card (mounted drive)
ssd    = "E:\\CameraBackup"   # Local SSD destination
nas    = "Y:\\CameraBackup"   # NAS mapped via SMB (or WireGuard VPN)

file_extensions = [
  ".MOV",
  ".NEF",
  ".JPG",
  ".MP4",
]

video_extensions = [
  ".MOV",
  ".MP4",
]
